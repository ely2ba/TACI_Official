{
    "oneOf": [
      { "$ref": "#/definitions/arrayForm" },
      {
        "type": "object",
        "required": ["actions"],
        "properties": { "actions": { "$ref": "#/definitions/arrayForm" } },
        "additionalProperties": false
      }
    ],
    "definitions": {
      "arrayForm": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "required": ["action", "selector"],
          "properties": {
            "action":   { "type": "string", "minLength": 1 },
            "selector": { "type": "string"                 },
            "text":     { "type": "string"                 },
            "value":    { "type": "string" }   
          },
          "additionalProperties": false
        }
      }
    }
  }
  